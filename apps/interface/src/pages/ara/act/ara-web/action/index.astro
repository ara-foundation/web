---
/**
 * @this Page
 * @param {string} Title Ara Web Actions
 * @param {string} Description Customize or browse all available Actions in Ara Web
 */
import { newRpcCall, type RpcCallType } from "@scripts/rpc/extensions/redirect"
import Call from "@components/rpc/call.astro";

let rpcCall: RpcCallType = newRpcCall();
const redirectUrl = "/ara/act/ara-web/action/get"
rpcCall.inputs = [redirectUrl]
rpcCall = {...rpcCall, inputs: [redirectUrl]}

---

<Call rpcCall={rpcCall} />