---
/**
 * @this Page
 * @param {string} Title Ara Web Actions
 * @param {string} Description Customize or browse all available Actions in Ara Web
 */
import { AstroComponentCall, ExtensionsRedirect } from "@ara-web/rpc-engine";

let rpcCall: ExtensionsRedirect.RpcCallType = ExtensionsRedirect.newRpcCall();
const redirectUrl = "/ara/act/ara-web/action/get"
const inputs: [string] = [redirectUrl]
rpcCall = {...rpcCall, inputs}
console.log(`Console log is not handled`)

---

<AstroComponentCall rpcCall={rpcCall} />