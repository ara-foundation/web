---
/**
 * @this Page
 * @param {string} Title Ara Web's Component's REST operations
 * @param {string} Description Section of Ara Web to handle web components
 */
import { newRpcCall } from "@scripts/rpc/extensions/redirect"
import Call from "@components/rpc/call.astro";

const rpcCall = newRpcCall();
const redirectUrl = "/ara/act/ara-web/component/get"
rpcCall.inputs = [redirectUrl]

---

<Call rpcCall={rpcCall} />