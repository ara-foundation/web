---
// An extension that redirects to another project
import Extension from "@components/rpc/extension/index.astro"
import {type ExtensionType} from "@scripts/rpc";

interface Props extends ExtensionType {}

---

<Extension 
    {...Astro.props}
    name="Alert"
    description="Show a popup"
></Extension>