---
import Reflect from '@scripts/reflect';
interface Props {
    url?: string
}

const {url} = Astro.props;

const page = await Reflect.getPageByUrl(url);
const pageTitle: string = page === undefined ? "No Title" : page.title 
---

<p>{pageTitle}</p>
