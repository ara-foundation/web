# The decorators
The only decorator is in the `@ara-web/reflect/code-level/value-level/value-level-interface.js`.
This decorator allows creating interfaces with the static methods.

# Todo
Move the interface decorator to the Ts-Enhancement.
Create another decorator called @todo() allowing to print a todo in the logs.