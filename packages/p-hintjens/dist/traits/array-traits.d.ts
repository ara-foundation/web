/**
 * Useful methods to work with the Arrays that Typescript doesn't have,
 * and downloading the different package isn't worth it.
 */
export declare class ArrayTraits {
    static isEqualArray: (a: any[], b: any[]) => boolean;
}
